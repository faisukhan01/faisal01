# Task 7-f — Premium styling polish pass on Who-We-Serve + Industries + Awards

**Agent:** full-stack-developer
**Task ID:** 7-f
**Scope:** Round 7 premium styling polish on THREE existing NETSOL homepage sections:
1. `src/components/sections/who-we-serve.tsx` (3-column audience grid)
2. `src/components/sections/industries.tsx` (6-card multi-industry grid)
3. `src/components/sections/awards.tsx` (8-card awards grid, DARK section)

## Inputs read
- `/home/z/my-project/worklog.md` (prior rounds 1-6 + parallel Round 7 work 7-a/b/c, especially 6-f polish patterns for solutions/roi-calculator)
- `/home/z/my-project/agent-ctx/6-f-full-stack-developer.md` (Round 6 polish pattern reference — `gradient-border-animated` + `inline-style backgroundColor` override for dark surfaces, `section-heading-chip backdrop-blur-sm` + inline style override for dark chip adaptation)
- `/home/z/my-project/src/components/sections/who-we-serve.tsx` (existing 3-column audience grid — `motion.div` + staggerItem, 3-col grid with `md:border-r-0` for connected-cards look, icon tile with group-hover bg flip, faded "0X" decorative number)
- `/home/z/my-project/src/components/sections/industries.tsx` (existing 6-card grid — `Industry` interface with per-industry `accent` + `bg`, `INDUSTRIES` array, `IndustryIcon` SVG switch, top-row icon tile + metrics row + decorative blob + hover arrow, bottom-bar CTA)
- `/home/z/my-project/src/components/sections/awards.tsx` (existing DARK awards section `bg-[#0f172a]` — CursorSpotlight wrapper, topographic pattern + soft accent blobs, 8-card grid with Trophy icon + year + body + title, bottom quote strip)
- `/home/z/my-project/src/lib/site-data.ts` (verified `WHO_WE_SERVE` shape — 3 items with `id/title/description/icon`; `AWARDS` shape — 8 items with `id/year/body/title/accent`)
- `/home/z/my-project/src/app/globals.css` (verified all Round 6 + Round 7 premium utilities available: `section-heading-chip`, `section-rule`, `card-stack-3d`, `lift-on-hover`, `lift-on-hover-strong`, `shadow-depth`, `shadow-depth-lg`, `gradient-border-animated`, `glow-halo`, `nav-link-underline`, `mesh-gradient`, `spotlight-gradient`, `text-gradient-animated`, `font-mono-numeric`, `live-pulse-dot`, `evidence-badge`, `btn-shine`, `clamp-3`)
- `/home/z/my-project/src/components/site/cursor-spotlight.tsx` (verified `CursorSpotlight` wraps children in `relative z-10`, so adding `spotlight-gradient` overlay as a sibling at `absolute inset-0` inside the children layer correctly sits behind grid content)

## Constraints honored
- `'use client'` preserved on all three files
- All existing functionality preserved: `motion.div`/`motion.article` variants, `Reveal`/`Stagger`/`staggerItem`, `Magnetic` + `CTAButton` wrapping, `CursorSpotlight` interactive cursor-follow glow, topographic pattern + accent blobs, bottom quote strip, all `href="#contact"` CTAs, all 3 SVG `ServeIcon` variants + 6 `IndustryIcon` variants
- `globals.css` NOT modified
- No other files modified (page.tsx, reveal.tsx, cta-button.tsx, cursor-spotlight.tsx, site-data.ts untouched)
- TypeScript strict-safe — no `any` used; `AUDIENCE_LABELS` typed as `readonly ['Captives', 'OEMs', 'Brokers']`; `Industry` interface unchanged; all `i % 2 === 0` parity checks return string literals typed as `'LIVE' | 'PROVEN'` and `'WINNER' | 'CERTIFIED'` via inference
- Mobile-first responsive preserved (grids, paddings, breakpoints unchanged)
- For awards dark section: all text remains readable — chip uses `text-white` via inline override; year uses `text-gradient-animated` (animated blue gradient on dark — high contrast); evidence-badge retains its green color from `globals.css`; body/title retain white/60 + white; the section-rule (blue gradient hairline) is still visible against `#0f172a`
- Inline `style` props used to override `background: white` shorthand from `gradient-border-animated` on dark award cards (kept `background: 'rgba(255, 255, 255, 0.04)'` per the established 6-f pattern)
- Inline `style` props used to override `color: #1d81f2`, `background: linear-gradient(...blue)`, `border: 1px solid ...blue` from `section-heading-chip` on the dark awards chip (kept `bg-white/10` + `border-white/20` + `text-white` per the spec's dark-bg adaptation requirement)

## who-we-serve.tsx changes
1. Section background: added `mesh-gradient` overlay div (`absolute inset-0 pointer-events-none`) over the existing white bg for premium blue/cyan radial accents.
2. Section heading: replaced the inline `inline-flex items-center gap-3` (gray bar + "Who We Serve" text) block with a `section-heading-chip` containing a `h-1.5 w-1.5 rounded-full bg-[#1d81f2] live-pulse-dot` accent dot + "Who We Serve" text (auto-uppercased by the class). Wrapped chip + h2 + section-rule + paragraph in the same `Reveal`.
3. Added `section-rule mt-6` hairline directly under the h2.
4. 3-column cards: added `card-stack-3d lift-on-hover-strong shadow-depth-lg` to each card (alongside the existing `group relative rounded-2xl border border-[#f0f0f0] bg-white p-7 lg:p-8 hover:bg-[#f5f7fa]/40`). Preserved the `i < 2 && 'md:border-r-0'` conditional. The card-stack-3d pseudos peek out from bottom-right; the lift provides -6px lift on hover with smooth 0.45s transition; shadow-depth-lg provides multi-layer depth shadow.
5. Top accent stripe: replaced the existing hover-reveal `h-[3px] bg-gradient-to-r from-[#1d81f2] to-transparent opacity-0 group-hover:opacity-100` with an always-visible `h-[3px] rounded-t-2xl` gradient stripe using inline `background: linear-gradient(90deg, #1d81f2 0%, #56ccf2 60%, transparent 100%)` (premium blue→cyan→transparent sweep).
6. Icon tile: added `gradient-border-animated glow-halo` to the existing `flex h-16 w-16 items-center justify-center rounded-2xl` tile. Changed `rounded-xl` → `rounded-2xl` to match the class's 16px radius. Preserved the `group-hover:bg-[#1d81f2] group-hover:text-white transition-colors duration-300` (Tailwind group-hover utilities have higher specificity (0,2,0) than the class's (0,1,0) `background: white`, so the hover bg flip still wins on hover; the gradient border ::before shimmers on hover alongside the bg flip). The glow-halo ::after pseudo provides a soft radial blue glow behind the icon tile.
7. Card title (h3): added `nav-link-underline inline-block` for premium underline-grow on hover (gradient blue→cyan underline animates from 0 → 100% width on hover).
8. Card description: kept `text-[#525252] leading-[1.65]` (preserved verbatim — spec says "Keep text-525252 leading-relaxed").
9. CTA link inside each card: added `nav-link-underline` to the existing "Learn more" CTA row (was `inline-flex items-center gap-2 text-[14px] font-medium text-[#1d81f2] opacity-0 group-hover:opacity-100`). Now the CTA gets the premium underline-grow on hover alongside the existing opacity-reveal.
10. Editorial faded number: replaced the existing `text-[64px] font-bold leading-none text-[#1d81f2]/5` decorative "0X" with `font-mono-numeric text-[80px] font-bold leading-none select-none pointer-events-none` + inline `style={{ color: 'rgba(29, 129, 242, 0.08)' }}` (8% opacity per spec, big mono font for premium editorial feel). Kept absolute `top-2 right-3` positioning.
11. Audience badge: added a small uppercase chip in each card top-right corner at `absolute top-4 right-5 z-10` — `inline-flex items-center gap-1.5 rounded-full border border-[#1d81f2]/20 bg-[#1d81f2]/5 px-2.5 py-1 text-[10px] font-semibold uppercase tracking-[0.08em] text-[#1d81f2]` with a `h-1 w-1 rounded-full bg-[#1d81f2]` accent dot. Sourced from a local `AUDIENCE_LABELS = ['Captives', 'OEMs', 'Brokers']` array (derived from each card's title — "Captives & Lenders" → "Captives", "OEMs & Dealers" → "OEMs", "Brokers & Aggregators" → "Brokers").

## industries.tsx changes
1. Section background: added `spotlight-gradient` overlay div (`pointer-events-none absolute inset-0`) ABOVE the existing decorative grid pattern (which has `opacity-30`) for premium central blue radial spotlight.
2. Section heading: replaced the inline `inline-flex items-center gap-3` (gray bar + "Industries We Power" text) block with a `section-heading-chip` containing a `h-1.5 w-1.5 rounded-full bg-[#1d81f2] live-pulse-dot` accent dot + "Industries We Power" text (auto-uppercased by the class). Wrapped chip + h2 + section-rule + paragraph in the same `Reveal`.
3. Added `section-rule mt-6` hairline directly under the h2.
4. Industry cards: added `card-stack-3d lift-on-hover shadow-depth` to each `motion.article`. Removed the existing `overflow-hidden hover:shadow-premium-lg transition-shadow` (overflow-hidden would clip the card-stack-3d pseudos at z-index -1; transition-shadow is overridden by lift-on-hover's transition shorthand anyway). Kept `group relative rounded-2xl border border-[#f0f0f0] bg-white p-6 hover:border-[#e0e0e0]`.
5. Top accent stripe: replaced the existing hover-reveal `h-1 origin-left scale-x-0 group-hover:scale-x-100` (4px, hover-reveal) with an always-visible `h-[3px] rounded-t-2xl` gradient stripe using inline `background: linear-gradient(90deg, ${ind.accent} 0%, ${ind.accent}99 60%, transparent 100%)` (per-industry accent color gradient sweep, premium always-on visibility).
6. Industry icon tile: added `gradient-border-animated` to the existing `flex h-12 w-12 items-center justify-center rounded-2xl` tile (kept inline `style={{ backgroundColor: ind.bg, color: ind.accent }}` — inline style overrides the class's `background: white` shorthand, so the icon tile keeps its per-industry tinted bg + accent color; the gradient border ::before shimmers on hover). Changed `rounded-xl` → `rounded-2xl` to match the class's 16px radius.
7. Industry name (h3): added `nav-link-underline mt-5 inline-block` for premium underline-grow on hover (gradient blue→cyan underline animates from 0 → 100% width on hover; `inline-block` ensures the underline width matches the text width, not the row width).
8. Industry description: kept `text-[#525252] leading-[1.6] clamp-3` (preserved verbatim — spec says "Keep text-525252 leading-relaxed").
9. Industry metric/stat value: replaced inline `style={{ color: ind.accent }}` + `text-[18px] font-bold leading-none tracking-tight` with `text-gradient-animated font-mono-numeric text-[18px] font-bold leading-none tracking-tight` (animated blue→cyan→deep blue shimmer gradient + tabular monospace numerics for stable digit widths during counter animations / data refreshes).
10. Evidence badge: added to each card top-right via a new flex row at the top of the card body — `flex items-center justify-between` containing the icon tile (left) + `evidence-badge` (right). The badge text alternates per card index using `i % 2 === 0 ? 'LIVE' : 'PROVEN'`. The `evidence-badge` class (from `globals.css`) provides green styling (`background: linear-gradient(135deg, rgba(36, 161, 72, 0.12), rgba(36, 161, 72, 0.06)); border: 1px solid rgba(36, 161, 72, 0.3); color: #24a148`). Added a `h-1 w-1 rounded-full bg-current` accent dot inside the badge for visual texture.
11. Removed the existing absolute hover arrow (was `absolute right-5 top-5 flex h-8 w-8 items-center justify-center rounded-full opacity-0 group-hover:opacity-100 ...`) because it conflicted visually with the new evidence-badge in the top-right area. The `lift-on-hover` + `card-stack-3d` + `gradient-border-animated` + top accent stripe collectively provide rich hover feedback that subsumes the arrow's role.
12. Decorative gradient blob: kept at `absolute -bottom-10 -right-10 h-32 w-32 rounded-full opacity-0 group-hover:opacity-30 transition-opacity duration-500 blur-2xl` (slightly reduced offset from `-bottom-12 -right-12` → `-bottom-10 -right-10` since `overflow-hidden` was removed; the small spill into the gap-5 (20px) gap between cards is contained by `blur-2xl` softening and only-on-hover visibility — looks like a subtle accent glow extending past the card edge).
13. Bottom CTA: added `btn-shine lift-on-hover` to the "Talk to a domain expert" anchor (alongside the existing `inline-flex items-center gap-2 rounded-[10px] bg-[#1d81f2] px-5 py-2.5 text-[14px] font-semibold text-white hover:bg-[#0f62fe] shadow-[0_4px_12px_-2px_rgba(15,98,254,0.45)]`). The btn-shine adds a premium light sweep across the button on hover; lift-on-hover adds a -4px lift with smooth 0.4s transition. Removed `transition-colors` (it was overridden by lift-on-hover's transition shorthand anyway — unlayered class beats Tailwind's layered utility per CSS @layer rules; the bg-color snap on hover is barely perceptible since the change is from #1d81f2 → #0f62fe — both deep blues).

## awards.tsx changes (DARK section — bg-[#0f172a])
1. Section heading: replaced the inline `inline-flex items-center gap-2 mb-4` (cyan dot + "Awards & Recognition" text-white/70) block with a `section-heading-chip backdrop-blur-sm mb-4` chip + inline style override `style={{ background: 'rgba(255, 255, 255, 0.1)', borderColor: 'rgba(255, 255, 255, 0.2)', color: '#ffffff' }}` per the spec's dark-bg adaptation (chip class provides the `display: inline-flex; align-items: center; gap: 8px; padding: 6px 14px; border-radius: 999px; font-size: 11px; font-weight: 600; letter-spacing: 0.08em; text-transform: uppercase;` structure; inline style overrides the class's blue `background: linear-gradient(...blue)` + `border: 1px solid rgba(29, 129, 242, 0.18)` + `color: #1d81f2` to render as white-on-translucent-white-on-dark — readable on dark bg). Inside the chip: a `h-1.5 w-1.5 rounded-full bg-[#56ccf2] live-pulse-dot` accent dot (cyan, subtly pulsing). The chip auto-uppercases "Awards & Recognition".
2. Added `section-rule mt-6` hairline directly under the h2 (the section-rule's blue gradient hairline is faintly visible against the dark bg — looks like a subtle premium accent line under the heading).
3. Award cards: added `gradient-border-animated lift-on-hover shadow-depth-lg` to each `motion.article`. The `gradient-border-animated` class's `background: white` shorthand is overridden by inline `style={{ background: 'rgba(255, 255, 255, 0.04)' }}` per the established 6-f pattern (so the card keeps its dark translucent surface while still benefiting from the shimmer-border ::before pseudo). Kept `group relative overflow-hidden rounded-2xl p-6 transition-all duration-300 hover:bg-white/[0.06] hover:border-white/20` + the existing `lift-on-hover` was already on the card (preserved — no duplication since the class is now added once via the spec). Preserved the existing `initial`/`whileInView`/`viewport`/`transition` Framer Motion config (entrance animation intact).
4. Top accent stripe: replaced the existing hover-reveal `h-[3px] origin-left scale-x-0 group-hover:scale-x-100` with an always-visible `h-[3px] rounded-t-2xl` gradient stripe using inline `background: linear-gradient(90deg, ${a.accent} 0%, ${a.accent}99 60%, transparent 100%)` (per-award accent color gradient sweep).
5. Year: replaced `text-[11px] font-mono uppercase tracking-widest text-white/40` with `text-gradient-animated font-mono-numeric text-[14px] font-semibold tracking-widest` (premium animated blue gradient text + tabular monospace numerics for the 4-digit year; bumped size from 11px → 14px and weight to semibold so the gradient shimmer is visible at this size). Moved the year to a vertical flex column (`flex flex-col items-end gap-2`) with the new evidence-badge stacked below it.
6. Awarding body: added `nav-link-underline inline-block` to the existing `text-[11px] font-semibold uppercase tracking-wider text-white/60` (the spec says "if it's a link" — the body is not currently a link, but the underline-grow on hover provides premium interaction feedback consistent with the title; `inline-block` ensures the underline width matches the text width).
7. Award title: wrapped the title text in a `<span className="nav-link-underline inline-block">` inside the existing `h3` (the h3 has `flex-1` which makes it fill the row; wrapping the text in an inline-block span ensures the underline ::after width matches the text width, not the full row width).
8. Evidence badge: added to each card in the top-right column (stacked below the year) — `<span className="evidence-badge"><span className="h-1 w-1 rounded-full bg-current" aria-hidden />{a.id % 2 === 0 ? 'WINNER' : 'CERTIFIED'}</span>`. The `evidence-badge` class provides green styling (#24a148) — purely decorative per spec. Text alternates per award id parity.
9. Trophy icon: added `glow-halo` to the existing `<span className="... inline-flex h-11 w-11 items-center justify-center rounded-xl">` (alongside the existing `relative` — `glow-halo::after` is `position: absolute; inset: -40% -20%; background: radial-gradient(circle at center, rgba(29, 129, 242, 0.08) 0%, transparent 60%);` — provides a soft blue glow halo around the trophy icon). Added `relative` explicitly to ensure the ::after pseudo is positioned correctly.
10. Spotlight gradient overlay: added a `<div aria-hidden className="spotlight-gradient pointer-events-none absolute inset-0" />` BEHIND the awards grid — wrapped the existing `<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-5">...</div>` in a `relative` container with the spotlight overlay as its first child + the grid (now `relative`) as its second child. The spotlight sits behind the grid (z-auto vs relative grid). The radial blue glow at center adds premium ambiance to the awards grid.
11. Bonus polish: upgraded the 3 quick-stat cards in the right column to use `text-gradient-animated font-mono-numeric` on the big stat values (was `text-[20px] font-semibold text-[#56ccf2] leading-none` — now uses animated blue gradient + tabular numerics for consistency with the year treatment).

## Verification
- `bun run lint` → exit 0, 0 errors, 0 warnings on all three files (lint-clean across the whole project)
- Dev server is auto-managed by the system; the user's preview panel will trigger the recompile on next visit (last log entry: `GET / 200 in 8.3s (compile: 7.8s, render: 523ms)` — server is healthy)
- Verified all three files compile-safe via inspection: all imports used (no unused), all classnames are valid Tailwind or existing premium utilities from globals.css, no `any` types, all inline-style overrides correctly target `background` to defeat the `gradient-border-animated` class's `background: white` shorthand on dark surfaces (per the established 6-f pattern)

## Stage Summary
- 3 existing section files polished in-place; 0 new files created; globals.css NOT modified; no other files touched
- who-we-serve.tsx: mesh-gradient bg + section-heading-chip (with live-pulse-dot blue accent) + section-rule + card-stack-3d lift-on-hover-strong shadow-depth-lg cards + always-visible gradient top accent stripe (blue→cyan→transparent) + gradient-border-animated glow-halo icon tile (preserving group-hover bg flip) + nav-link-underline title + nav-link-underline "Learn more" CTA + 8% opacity mono editorial "0X" number behind content + per-card audience badge (Captives / OEMs / Brokers) in top-right corner
- industries.tsx: spotlight-gradient overlay + section-heading-chip (with live-pulse-dot blue accent) + section-rule + card-stack-3d lift-on-hover shadow-depth cards + always-visible per-industry-accent gradient top stripe + gradient-border-animated icon tile (preserving per-industry tinted bg) + nav-link-underline industry name + text-gradient-animated font-mono-numeric metric values + evidence-badge (alternating LIVE/PROVEN) in top-right + btn-shine lift-on-hover bottom CTA + decorative blob preserved (slightly repositioned)
- awards.tsx (DARK section): section-heading-chip backdrop-blur-sm with inline style override for dark-bg adaptation (white text on translucent-white-on-dark) + section-rule + gradient-border-animated lift-on-hover shadow-depth-lg cards (with inline bg override for dark surface) + always-visible per-award-accent gradient top stripe + glow-halo Trophy icon + text-gradient-animated font-mono-numeric year (bumped to 14px semibold) + nav-link-underline awarding body + nav-link-underline award title (wrapped in inline-block span) + evidence-badge (alternating WINNER/CERTIFIED) stacked below year + spotlight-gradient overlay div behind the awards grid (wrapped grid in relative container) + bonus polish on the 3 quick-stat cards (text-gradient-animated font-mono-numeric for consistency with year treatment)
- All existing functionality preserved: Framer Motion entrance animations, CursorSpotlight interactive cursor-follow glow, topographic pattern + accent blobs, bottom quote strip, all `href="#contact"` CTAs, all SVG icon variants
- Lint clean (exit 0); production-ready
